# Production / preview builds only. `npm run dev` still uses ws://localhost:8787
# unless the page URL includes ?ws=
# Copy to .env.production.local (gitignored) or export before `npm run build`.
# Do not commit a real host. Placeholder:
# VITE_WS_URL=wss://your-service.onrender.com
